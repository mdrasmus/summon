#AR = ar
PREFIX = ..

INCLUDE = \
    -I$(PREFIX)/Common \
    -I$(PREFIX)/GlutMVC \
    -I$(PREFIX)/MVC 

MY_LIBS = \
        $(PREFIX)/Common/libCommon.a \
        $(PREFIX)/GlutMVC/libGlutMVC.a \
        $(PREFIX)/MVC/libMVC.a 

#  -DNDEBUG

COMPILE = -c -g \
    -Wall -g -fno-strict-aliasing -O2 -fPIC -Wstrict-prototypes  \
    $(INCLUDE) 

# Profiling code
ifdef PROFILE
    COMPILE := $(COMPILE) -pg 
endif

#
# Python COMPILE flags
#
COMPILE := $(COMPILE) -I/usr/include/python2.4 -I/usr/include/python2.3

# test compiling
test:
	echo $(COMPILE)


# rules for subsystems
$(PREFIX)/Common/libCommon.a:
	make -C $(PREFIX)/Common
    
$(PREFIX)/GlutMVC/libGlutMVC.a:
	make -C $(PREFIX)/GlutMVC

$(PREFIX)/MVC/libMVC.a:
	make -C $(PREFIX)/MVC




# basic compile rule
$(OBJS): %.o: %.cpp
	$(CC) $(COMPILE) -o $@ $<

# basic library building
$(LIB): $(OBJS)
	$(AR) -r $(LIB) $(OBJS)

# basic clean rule
clean:
	rm -rf $(OBJS) $(LIB) $(PROG)

cleanobj:
	rm -rf $(OBJS) $(LIB)

cleanall:
	make clean -C $(PREFIX)/Common
	make clean -C $(PREFIX)/GlutMVC
	make clean -C $(PREFIX)/MVC
	make clean -C $(PREFIX)/Summon

cleanallobj:
	make cleanobj -C $(PREFIX)/Common
	make cleanobj -C $(PREFIX)/GlutMVC
	make cleanobj -C $(PREFIX)/MVC
	make cleanobj -C $(PREFIX)/Summon

